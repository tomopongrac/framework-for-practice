default:
    extensions:
        Behat\MinkExtension\ServiceContainer\MinkExtension:
            base_url:  'http://localhost:80'
            sessions:
                default:
                    goutte: ~
